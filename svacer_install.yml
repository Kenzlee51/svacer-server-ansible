---

- name: install svacer
  hosts: "{{ target | default('local')  }}"
  become: true
  vars_file:
    - vars.yml

  roles:
    - role: environment_preparation
      tags:
        - always

    - role: svacer_server_launch
      tags:
        - always
    